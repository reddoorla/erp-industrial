# Contact form → central @reddoorla/maintenance ingest (Airtable Submissions + dashboard
# + status-aware notify). The slug is PLURAL: erp-industrials.
FORMS_INGEST_URL=https://reddoor-maintenance.netlify.app/api/forms/erp-industrials
FORMS_INGEST_TOKEN=replace-with-the-shared-forms-ingest-token

# Prismic environment override (optional).
# VITE_PRISMIC_ENVIRONMENT=
